#include <stdio.h>
long   fib( int  n )
   { if (n>2) 
       return (fib(n-1)+fib(n-2));
     else
       return (1);
    }
  int main(void)
  {int i;
  for(i=0;i<20;i++)
  printf("%ld\n",fib(i)); 
   return 0; 
  }

